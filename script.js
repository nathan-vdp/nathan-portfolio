var typed = new Typed('.typed', {
    strings: ["Developer"],
    typeSpeed: 100,
    showCursor: false,
});

var typed = new Typed('.aboutme', {
    strings: ["Developer", "Student"],
    typeSpeed: 100,
    backSpeed: 100,
    loop: true,
    showCursor: false,
});